{
  "name": "leader-course-game",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve public",
    "deploy": "firebase deploy --project leader-game",
    "deploy:hosting": "firebase deploy --project leader-game --only hosting",
    "deploy:rules": "firebase deploy --project leader-game --only database"
  }
}
